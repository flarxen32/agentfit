/**
 * Placeholder test home for the scoring engine.
 *
 * The full test suite lands in XRO-14 alongside the real classifier/ROI
 * logic. This file exists to establish the test convention and directory.
 *
 * It intentionally avoids importing a test runner until XRO-14 adds one
 * (vitest) as a devDependency, so the skeleton typechecks cleanly.
 */
